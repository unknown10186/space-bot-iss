# space-bot-iss

git init
git remote add origin https://github.com/YOUR_USERNAME/space-bot-iss.git
git add .
git commit -m "Initial commit: project setup"
git branch -M main
git push -u origin main

git add space_iss.py
git commit -m "feat: added ISS API integration"
git push

